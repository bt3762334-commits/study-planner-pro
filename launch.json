{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Vite + Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/client",
      "preLaunchTask": "npm: dev"
    }
  ]
}
